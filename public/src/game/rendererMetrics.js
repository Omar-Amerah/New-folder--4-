const HISTORY_LIMIT = 360;
const PHASES = ["startup", "warmup", "steady", "transition", "cleanup"];
const phaseBaseline = () => ({ frameCount: 0, totalMs: 0, worstMs: 0, over16: 0, over33: 0, over50: 0, samples: [], callbackCount: 0, callbackTotalMs: 0, callbackWorstMs: 0, callbackOver33: 0, callbackOver50: 0, callbackSamples: [], lastCallbackAt: null });
const baseline = () => ({ frameCount:0, peakEntityViews:0, peakPoolCount:0, textureCreations:0, textureDestructions:0, structuralRebuilds:0, staticMapRebuilds:0, fatalFrameStage:null, frames:[], phases: Object.fromEntries(PHASES.map((p)=>[p, phaseBaseline()])), currentPhase: "startup", resetAt: 0 });
let metrics = baseline();
function finite(value, fallback = 0) { return Number.isFinite(value) ? value : fallback; }
function percentile(samples, p) { if (!samples.length) return 0; const sorted = [...samples].sort((a,b)=>a-b); return sorted[Math.min(sorted.length-1, Math.floor((sorted.length-1)*p))]; }
function summarize(bucket) { const n = bucket.frameCount || 0; return { samples: n, measuredFrames: n, averageMs: n ? bucket.totalMs / n : 0, p50Ms: percentile(bucket.samples, 0.50), p95Ms: percentile(bucket.samples, 0.95), p99Ms: percentile(bucket.samples, 0.99), worstMs: bucket.worstMs || 0, framesOver16_7Ms: bucket.over16 || 0, framesOver33Ms: bucket.over33 || 0, framesOver50Ms: bucket.over50 || 0 }; }
function summarizeCallbacks(bucket) { const n = bucket.callbackCount || 0; return { samples: n, averageMs: n ? bucket.callbackTotalMs / n : 0, p50Ms: percentile(bucket.callbackSamples, 0.50), p95Ms: percentile(bucket.callbackSamples, 0.95), p99Ms: percentile(bucket.callbackSamples, 0.99), worstMs: bucket.callbackWorstMs || 0, intervalsOver33Ms: bucket.callbackOver33 || 0, intervalsOver50Ms: bucket.callbackOver50 || 0, callbackHz: bucket.callbackTotalMs > 0 ? (n * 1000) / bucket.callbackTotalMs : 0 }; }
export function resetRendererMetrics(phase = "startup"){ metrics = baseline(); metrics.currentPhase = PHASES.includes(phase) ? phase : "startup"; metrics.resetAt = performance?.now?.() || Date.now(); }
export function setRendererMetricsPhase(phase, { reset = false } = {}) { if (!PHASES.includes(phase)) return metrics.currentPhase; metrics.currentPhase = phase; if (reset) metrics.phases[phase] = phaseBaseline(); return metrics.currentPhase; }
export function recordRendererFrame(sample={}){
  const durationMs = finite(sample.durationMs, 0);
  const phase = PHASES.includes(sample.phase) ? sample.phase : metrics.currentPhase;
  metrics.frameCount += 1;
  metrics.peakEntityViews = Math.max(metrics.peakEntityViews, sample.entityViews || 0);
  metrics.peakPoolCount = Math.max(metrics.peakPoolCount, sample.poolCount || 0);
  if (sample.fatalFrameStage) metrics.fatalFrameStage = sample.fatalFrameStage;
  const bucket = metrics.phases[phase] || metrics.phases.steady;
  bucket.frameCount += 1; bucket.totalMs += durationMs; bucket.worstMs = Math.max(bucket.worstMs, durationMs); if (durationMs > 16.7) bucket.over16 += 1; if (durationMs > 33) bucket.over33 += 1; if (durationMs > 50) bucket.over50 += 1; bucket.samples.push(durationMs); if (bucket.samples.length > HISTORY_LIMIT) bucket.samples.shift();
  const at = finite(sample.at, performance?.now?.() || Date.now());
  if (bucket.lastCallbackAt != null) { const interval = Math.max(0, at - bucket.lastCallbackAt); bucket.callbackCount += 1; bucket.callbackTotalMs += interval; bucket.callbackWorstMs = Math.max(bucket.callbackWorstMs, interval); if (interval > 33) bucket.callbackOver33 += 1; if (interval > 50) bucket.callbackOver50 += 1; bucket.callbackSamples.push(interval); if (bucket.callbackSamples.length > HISTORY_LIMIT) bucket.callbackSamples.shift(); }
  bucket.lastCallbackAt = at;
  metrics.frames.push({ at, durationMs, phase, visible: sample.visible || 0, culled: sample.culled || 0, sceneChildren: sample.sceneChildren || 0, qualityProfile: sample.qualityProfile || "medium", rendererResolution: sample.rendererResolution || 1 });
  if (metrics.frames.length > HISTORY_LIMIT) metrics.frames.shift();
}
export function incrementRendererMetric(name, amount=1){ if (name in metrics) metrics[name] += amount; }
export function rendererMetricsSnapshot(extra={}){ const last = metrics.frames[metrics.frames.length-1] || {}; const phases = Object.fromEntries(Object.entries(metrics.phases).map(([k,v])=>[k, { rendererWorkMs: summarize(v), callbackIntervalMs: summarizeCallbacks(v), callbackHz: summarizeCallbacks(v).callbackHz, measuredFrames: v.frameCount, callbackSamples: v.callbackCount }])); return { frameCount: metrics.frameCount, measuredFrames: metrics.frameCount, currentPhase: metrics.currentPhase, phases, rollingFrameStats: summarize({ frameCount: metrics.frames.length, totalMs: metrics.frames.reduce((a,f)=>a+finite(f.durationMs),0), worstMs: Math.max(0,...metrics.frames.map(f=>finite(f.durationMs))), over16: metrics.frames.filter(f=>f.durationMs>16.7).length, over33: metrics.frames.filter(f=>f.durationMs>33).length, over50: metrics.frames.filter(f=>f.durationMs>50).length, samples: metrics.frames.map(f=>finite(f.durationMs)) }), activeEntityViews: extra.activeEntityViews || 0, peakEntityViews: metrics.peakEntityViews, activePoolCount: extra.activePoolCount || 0, peakPoolCount: metrics.peakPoolCount, visibleCount: last.visible || 0, culledCount: last.culled || 0, textureCreationCount: metrics.textureCreations, textureDestructionCount: metrics.textureDestructions, textureCacheEntries: extra.textureCacheEntries || 0, zeroLeaseEntries: extra.zeroLeaseEntries || 0, structuralRebuildCount: metrics.structuralRebuilds, staticMapRebuildCount: metrics.staticMapRebuilds, sceneChildCounts: extra.sceneChildCounts || {}, qualityProfile: last.qualityProfile || extra.qualityProfile || "medium", rendererResolution: last.rendererResolution || extra.rendererResolution || 1, fatalFrameStage: metrics.fatalFrameStage }; }
