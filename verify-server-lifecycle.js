const assert=require('assert'); const http=require('http'); const { createGameServer }=require('./server');
(async()=>{const g=createGameServer({port:0,host:'127.0.0.1'}); assert(g.diagnostics().stopped); await g.start(); assert(g.address().port>0); assert(g.diagnostics().activeTimers.includes('simulation')); await new Promise((res,rej)=>http.get(`http://127.0.0.1:${g.address().port}/`, r=>{r.resume(); res();}).on('error',rej)); assert.throws(()=>g.start(), /already started/); await g.stop(); await g.stop(); assert(g.diagnostics().stopped); console.log('server lifecycle ok');})().catch(e=>{console.error(e);process.exit(1);});
