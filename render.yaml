services:
  - type: web
    name: modular-fleet-arena
    runtime: node
    buildCommand: npm ci
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: WS_ALLOWED_ORIGINS
        sync: false
