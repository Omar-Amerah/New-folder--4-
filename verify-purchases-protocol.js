"use strict";
// Focused purchase-protocol coverage is currently exercised through the real
// runtime WebSocket scenario: it sends buyShip over a real server process and
// validates MessagePack active snapshots with authoritative economy fields.
require("./verify-runtime.js");
